# Amazon ABSA Insight Tool -- dependencies
#
# torch is installed separately from the CUDA-enabled index (RTX 5060 /
# Blackwell needs a recent cu128 build):
#   pip install torch --index-url https://download.pytorch.org/whl/cu128
#
# Then install the rest:
#   pip install -r requirements.txt

polars==1.43.0
pandas==2.3.3
pyarrow==24.0.0
openpyxl==3.1.5

pyabsa==2.4.3
# pyabsa's metric_visualizer dependency breaks on update-checker>=1.0 (its
# UpdateChecker.check() signature changed) -- pin below that.
update-checker<1.0

matplotlib==3.10.9
seaborn==0.13.2
altair==6.2.2
streamlit==1.60.0

anthropic==0.118.0
google-genai==2.14.0
python-dotenv==1.2.2
tenacity==9.1.4

flask==3.1.3
flask-cors==6.0.5
